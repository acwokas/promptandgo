# Status as of 7 Sept 2026

Written so a future session (or a crashed one) can pick this up from the
repo alone, not from chat history. Supersede this file's content by
editing docs/CONTENT_PIPELINE.md and docs/CONTENT_PLAN.md directly once
these items are resolved; this file is a snapshot, not a living doc.

## What's true right now, verified today (not inherited from an earlier note)

- The 27 Aug content-pipeline work was committed and pushed today. Two
  branches exist: `rescue/content-pipeline-27aug` (off `main`, includes an
  unrelated pre-existing avatar/XSS WIP found sitting in the working
  tree, already live on `astro` as commit `3a1e00d` so that WIP is likely
  redundant, not lost work) and `content-pipeline` (off `origin/astro`,
  the branch that's actually deployed — **this is the one that matters**).
- **PR open**: https://github.com/acwokas/promptandgo/pull/1
  (`content-pipeline` → `astro`). Cloudflare Pages preview build passed.
  `/glossary` verified live on the preview with all 23 terms. `/tips`
  could not be verified on the preview (see below) so it was instead
  verified directly against the production database with the
  publishable anon key already shipped in the live site's own JS bundle:
  the new staggering filter returns the identical 18 rows the old query
  did. No regression.
- **The two glossaries were reconciled, not just added to.** The 27 Aug
  session created a new file-based glossary without checking whether one
  already existed — it did (`astro/src/pages/glossary.astro`, live,
  14 unsourced terms), and both were silently compiling to the same
  `/glossary` route (Astro only warns on this today; "will be a hard
  error in following versions"). Fixed: merged, old file deleted, 23
  terms total, no live URL broken (only `/glossary/` itself was indexed;
  no per-term URLs existed to break).
- **The real production Supabase project ref is `dkdakwyrqyfdkyukqmqs`**,
  confirmed by decoding the anon JWT actually shipped in the live site's
  JS. A different ref, `sszxxmxqidkpkhlkstgs`, shows up in one uncommitted
  file (`astro/public/_headers`, unrelated pre-existing WIP, not part of
  the content-pipeline PR) and in this machine's local
  `supabase/.temp/project-ref` cache. That looks like a mistake from
  separate work, not the real project. Worth Adrian's attention, not
  acted on here since it's outside this task's scope.
- **No service-role credential for the real project exists on this
  machine**, checked thoroughly, not assumed: no matching file anywhere
  under `~/.aiia` (which holds every other estate site's keys, by name,
  and has nothing for promptandgo), and `supabase link --project-ref
  dkdakwyrqyfdkyukqmqs` fails with an explicit
  "account does not have the necessary privileges" error from Supabase's
  own API. **The two gated tip migrations remain unapplied.** Someone
  with real access needs to run `supabase db push` (after `supabase link
  --project-ref dkdakwyrqyfdkyukqmqs` succeeds for them), or hand this
  session a working service-role key.
- **No Cloudflare Pages deploy-hook access exists on this machine
  either.** The only Cloudflare token found (`~/.aiia/cloudflare-keys`)
  is explicitly scoped to Billing:Read and Account Analytics:Read only,
  documented as being for a specific admin dashboard, not Pages
  management. Confirmed by the token's own on-disk documentation, not
  tested destructively. `.github/workflows/scheduled-rebuild.yml` in the
  PR needs a `CLOUDFLARE_DEPLOY_HOOK_URL` repo secret Adrian has to add
  himself. **This is lower priority than it looks**: the normal weekly
  flow (a generated tip's PR gets merged to `astro`) already triggers a
  standard Cloudflare Pages production build on merge, confirmed live via
  the PR's own preview-build check. The scheduled-rebuild workflow only
  matters for the edge case of applying an already-merged, future-dated
  migration with no accompanying code push.
- **The commit-vs-rebuild question was tested, not inherited.** The
  argument for rebuilding was "no generation trigger exists, so every
  article needs hand-drafting forever." That gap was real but additive to
  fix, not a reason to discard working, tested code: a scheduled task
  (`promptandgo-weekly-tip`, cron `17 9 * * 2`, i.e. Tuesdays) now exists
  and will attempt its first real run 8 Sept. It dispatches on
  `claude-sonnet-5` (the estate's default tier, not Opus), never touches
  the metered Anthropic API key, and opens a PR rather than publishing
  directly. See docs/CONTENT_PIPELINE.md "Automated generation" for
  exactly what it does each run, and docs/CONTENT_PLAN.md for the
  cadence reasoning (one tip a week, not the original 2-3/week, given the
  site isn't active).
- **A real reliability caveat, found by reading the rest of this
  machine's ~50 other scheduled tasks, not assumed**: at least one
  comparable Mac-scheduled task (`arabia-daily-guide`) was disabled after
  landing only 9 of 14 days, with its own note attributing this to "a
  scheduled session mounts no folder and cannot reach credentials at fire
  time," and was replaced by a GitHub Actions CI job for reliability.
  That CI escape hatch isn't available here (CI would need the metered
  API key, which is deliberately dead). Other daily Mac-scheduled tasks
  in this same list (udo-daily-content, du-topical-watchdog-daily, etc.)
  do show consistent recent `lastRunAt` timestamps, so this isn't a
  guaranteed failure mode, but `promptandgo-weekly-tip` should be
  watched for the first several weeks rather than assumed reliable. If it
  misses runs the way arabia-daily-guide did, the fix is almost certainly
  the same one that fixed that: move it into whatever CI mechanism the
  estate ends up using for subscription-route generation, if one gets
  built, rather than adding estate-style P1 watchdogs to a single
  low-stakes weekly task.
- Sitemap claim in the brief this session started from ("lastmod of 6
  March on every tip URL") did not check out: the live sitemap has no
  `lastmod` field at all, on any URL. Noted for accuracy, not acted on.

## What still needs a human

1. Review and merge (or request changes on) PR #1.
2. Provide a working Supabase service-role credential for
   `dkdakwyrqyfdkyukqmqs`, or apply the two pending migrations directly
   (`supabase/migrations/20260831090000_tip_*.sql` and
   `20260902090000_tip_*.sql`) — once merged.
3. Optionally add `CLOUDFLARE_DEPLOY_HOOK_URL` as a repo secret (Settings
   → Secrets and variables → Actions) if the staggered-migration-without-
   a-fresh-push case matters enough to bother with now.
4. Look at the `sszxxmxqidkpkhlkstgs` reference in the uncommitted
   `astro/public/_headers` file and local Supabase CLI cache — likely a
   mistake from unrelated work, not verified further here.
5. Watch `content/tips/generation-log.md` after 8 Sept to see whether the
   scheduled task actually ran.
